#include <iostream>

using namespace std;

int main()
{
  cout << "NJuan!" << endl;

  //Check an open port
  //sf::TcpSocket socket;

  //bool open = (socket.connect(sf::IpAddress(address), port) == sf::Socket::Done);
  //socket.disconnect();




  //List ports from localhost



  return 0;
}